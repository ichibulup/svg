/** @type {import('next').NextConfig} */
const nextConfig = {
  /* config options here */
  transpilePackages: ["gorth-ui"],

  devIndicators: false,
  reactCompiler: true,
};

export default nextConfig;
