/** @type {import('next').NextConfig} */
const nextConfig = {
  /* config options here */
  transpilePackages: ["gorth-base-primitive"],

  devIndicators: false,
  // reactCompiler: true,
};

export default nextConfig;
